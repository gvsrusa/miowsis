import React from 'react';
import { Box, Typography } from '@mui/material';

const ESGImpact = () => {
  return (
    <Box>
      <Typography variant="h4">ESG Impact</Typography>
      <Typography variant="body1">ESG Impact page content goes here.</Typography>
    </Box>
  );
};

export default ESGImpact;