import React from 'react';
import { Box, Typography } from '@mui/material';

const Settings = () => {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography variant="body1">Settings page content goes here.</Typography>
    </Box>
  );
};

export default Settings;